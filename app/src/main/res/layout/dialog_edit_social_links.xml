<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- Заголовок списка ссылок -->
    <TextView
        android:id="@+id/tv_social_network_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Редактирование ссылок"
        android:textColor="@color/text"
        android:textSize="18sp"
        android:layout_marginBottom="8dp" />

    <!-- RecyclerView для списка добавленных ссылок -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_edit_links"
        android:layout_width="match_parent"
        android:layout_height="300dp"
        android:scrollbars="vertical"
        android:layout_marginBottom="16dp" />

    <!-- Кнопка добавления новой ссылки -->
    <Button
        android:id="@+id/btn_add_new_link"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="?attr/selectableItemBackground"
        android:text="Добавить новую ссылку"
        android:textColor="@color/text"
        android:textSize="16sp" />

</LinearLayout>
