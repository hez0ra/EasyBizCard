<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="CustomAlertDialog" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="colorAccent">@color/main</item> <!-- Цвет кнопки -->
        <item name="android:background">@color/background</item> <!-- Цвет фона -->
        <item name="android:textColor">@color/text</item> <!-- Цвет title -->
        <item name="android:textColorPrimary">@color/white</item> <!-- Цвет текста на кнопках "положительных" действий -->
        <item name="android:textColorSecondary">@color/white</item> <!-- Цвет текста на кнопках "отрицательных" действий -->
    </style>
    <style name="CustomPopupMenu" parent="Widget.AppCompat.PopupMenu">
        <item name="android:popupBackground">@color/white</item>
        <item name="android:textColor">@color/text</item>
        <item name="android:itemBackground">@color/background</item>
        <item name="android:buttonTint">@color/main</item>
    </style>
</resources>